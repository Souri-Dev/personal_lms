{% block title %}Add Student
{% endblock %}

{% block body %}
	<h1>Add New Student</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-primary">Save</button>
	{{ form_end(form) }}
{% endblock %}
