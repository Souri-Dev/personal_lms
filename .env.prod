# Production Environment Variables
APP_ENV=prod
APP_SECRET=change_this_to_a_random_secret_key

# Database (will be overridden by docker-compose environment variables)
DATABASE_URL="mysql://csit_dept_lms_user:csit_dept_lms_pass@mysql:3306/csit_dept_lms?serverVersion=8.0"

# Mailer (configure for production)
MAILER_DSN=null://null

# Messenger (if using)
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
